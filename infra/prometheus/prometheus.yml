global:
  scrape_interval:     15s
  evaluation_interval: 15s

scrape_configs:
  # Prometheus itself
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Python services — expose /metrics once prometheus_client is added
  # (see issue: [performance] Expose Prometheus metrics from Python services)
  # - job_name: campus-processor
  #   static_configs:
  #     - targets: ["campus-processor:8000"]

  # - job_name: campus-analytics
  #   static_configs:
  #     - targets: ["campus-analytics:8001"]

  # - job_name: campus-bridge
  #   static_configs:
  #     - targets: ["campus-bridge:8002"]

  # Kafka consumer-lag metrics via kafka-exporter
  # (add campus-kafka-exporter service to docker-compose to enable)
  # - job_name: kafka
  #   static_configs:
  #     - targets: ["kafka-exporter:9308"]
