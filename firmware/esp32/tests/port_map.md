SHT30 VIN      → ESP32 3V3
SHT30 GND      → ESP32 GND
SHT30 SDA/RH   → ESP32 GPIO 21
SHT30 SCL/T    → ESP32 GPIO 22

IR A TX VCC    → ESP32 3V3
IR A TX GND    → ESP32 GND
IR A RX VCC    → ESP32 3V3
IR A RX GND    → ESP32 GND
IR A RX OUT    → ESP32 GPIO 32

IR B TX VCC    → ESP32 3V3
IR B TX GND    → ESP32 GND
IR B RX VCC    → ESP32 3V3
IR B RX GND    → ESP32 GND
IR B RX OUT    → ESP32 GPIO 33